# Materialist Dialectics 开发日志

本文件合并记录 Plan C（分析路径标签）与后续 UI 重设计的所有改动。

---

## 2026-04-17 08:42 +0800 — 日志文件创建

建立本日志文件，用于记录 Plan C 的每一次改动与时间戳。
记录位置：`tmp/materialist-dialectics/plan-c.log`

---

## 2026-04-17 08:45 +0800 — Plan C Step 1：后端 schema + 归一化

**目的**：先定后端契约，后续 prompt / 前端都围绕这个 schema 走。

**改动**：

1. `runtime/materialist-dialectics-api/lib/openai.js`
   - `RESPONSE_SCHEMA.meta.properties` 新增 `analysisPaths` 字段，
     类型 `array<string>`，items enum 6 条：
     `contradiction_analysis` / `concrete_analysis` / `primary_secondary`
     / `quantity_quality` / `practice_test` / `internal_external`
   - 加入 `required` 列表（OpenAI strict 模式要求所有 properties 都在 required 里）

2. `runtime/materialist-dialectics-api/api/materialist-dialectics/chat.js`
   - 新增 `ANALYSIS_PATH_ENUM` 常量 + `normalizeAnalysisPaths` 函数
     （白名单过滤、去重、最多 4 条）
   - `normalizeModelResult` 的返回值 meta 里多写一个 `analysisPaths`
   - fallback 对象同步补 `analysisPaths: []`
   - `directPolicyBlock` 的两个返回（自伤、违法）各补 `analysisPaths: []`

3. `runtime/materialist-dialectics-proxy/src/index.js`（Cloudflare Worker 镜像）
   - 以上所有改动同步一份，保持双后端契约一致
   - 含 OpenAI refusal 路径的 meta

**约束**：数量/频率约束（1–4 条）写在 prompt 层，不进 schema。
OpenAI strict 模式对 minItems/maxItems 支持有限，放 prompt 更稳。

**不破坏性验证**：
- 老字段 `questionType` / `disclaimer` 原样保留，只是多一个字段
- 前端若没读 `analysisPaths` 也完全不受影响
- DeepSeek 路径走 json_object 模式、不吃 schema，靠 prompt 约束

---

## 2026-04-17 08:50 +0800 — Plan C Step 2：prompt 指示模型回填 analysisPaths

**目的**：告诉模型这个新字段的含义、取值、填写原则与数量约束。
Schema 层解决"字段存在 + 值合法"，prompt 层解决"字段真实反映分析路径"。

**改动**：

1. `runtime/materialist-dialectics-api/lib/prompt.js`
   - 在原"# 十、输出格式"之前插入新的「# 十、analysisPaths 字段」整节：
     - 6 个合法取值 + 每条的中文含义说明（矛盾分析 / 具体问题具体分析
       / 主次矛盾 / 量变质变 / 实践检验 / 内因外因）
     - 填写规则：1–4 条；必须正文里真用到；
       轻问题 1 条 / 中问题 1–2 条 / 重问题 2–4 条；
       按支撑强度排序；reject / follow_up 时填空数组
     - 四条对照参考（决策困境、方法论、结构型历史、趋势判断）的典型组合
     - 反形式主义提醒：不要硬凑
   - 原"# 十、输出格式"重编号为「# 十一、输出格式」
   - 输出格式 JSON 样例里补上 analysisPaths 字段及其 enum
   - 五项自检不动（避免膨胀）

2. `runtime/materialist-dialectics-proxy/src/index.js`
   - 以上改动同步一份到 Worker 的镜像 buildSystemPrompt

**字节一致性验证**：
用 node eval 两边模板字面量并比对（处理转义差异），结果 IDENTICAL，
长度 7314 字符，双后端 prompt 完全同步。

**不破坏性验证**：
- 原有"# 六、disclaimer 字段"等编号都不变；只在末尾追加新节
- 新节写法与 disclaimer 字段节平行，模型熟悉这个模式
- 五项自检维持 5 条，不加第 6 条，避免 prompt 臃肿

---

## 2026-04-17 08:55 +0800 — Plan C Step 3：前端折叠展示

**目的**：在 assistant 的 `answer` 消息底下挂一个默认折叠的
「本次用到的分析路径」块，展开后显示保留的辩证法术语标签 + 通俗化解释。

**UI 形态**：原生 `<details>` / `<summary>` —— 最轻、零 JS、键盘可达、
浏览器内置折叠行为。

**文案**：标签保留辩证法原词（钩子 = 教学价值），
解释改为通俗版（老用户不反感、新用户能看懂）。六条最终文案：

- 矛盾分析：这件事里真正在打架的是什么？先找出来，再看谁是主角、谁是配角
- 具体问题具体分析：就这个人、这件事、这个处境来判断，不套通用公式
- 主次矛盾：分清哪个是根子问题、哪个只是表面，先解决根子
- 量变质变：小变化慢慢积累，到某个点突然变性质——看现在处在哪一段
- 实践检验：想不清的事，用行动去试一试，真假会自己暴露
- 内因外因：问题出在自己身上还是环境？外面的条件解释不通，就回头看自己

**改动**：

1. `assets/js/dialectics.js`
   - 新增 `ANALYSIS_PATHS` 常量（6 条，key + label + note）
   - 新增 `ANALYSIS_PATH_INDEX` 查表
   - 新增 `normalizeAnalysisPaths` 前端兜底（白名单 + 去重 + 最多 4 条）
   - `normalizeStoredMessage` 扩展：从 localStorage 读回时保留 analysisPaths
   - `createMessage` 扩展：多接一个 analysisPaths 参数
   - 新增 `createAnalysisPathsElement(paths)` 渲染 `<details>` + `<ul>`
   - `createMessageElement` 在 role=assistant && kind=answer && paths>0 时
     追加折叠块；追问 / 拒答 / 用户消息不挂
   - 接 API 响应处传入 `result.meta.analysisPaths`
   - `createExportPayload` 把分析路径附加写入导出的 markdown

2. `assets/css/dialectics.css`
   - 新增 `.dialectics-paths` 块样式：
     - 顶部虚线分隔、0.7rem 上边距
     - `<summary>` 等宽小字 + 自绘三角箭头，移除浏览器默认 marker
     - 打开时三角旋转 90deg（CSS transition）
     - 列表项：label 粗体 0.88rem，note 淡色 0.85rem
     - 复用现有 CSS 变量：`--d-hairline` / `--d-ink` / `--d-ink-soft` / `--font-mono`

**验证**：
- `node -c` 语法检查通过
- 本地 Hugo build 通过（Chinese 被转成 `\uXXXX` 转义字面量，无语义差）

**兼容性**：
- 旧 localStorage 里存的消息没有 analysisPaths → `normalizeStoredMessage`
  兜底成 `[]` → 条件判断过不去，旧消息不显示折叠块，完全不破坏
- `meta.analysisPaths` 在旧后端响应里不存在 → 兜底成 `[]` → 同上
- 追问 / 拒答消息按规则 `analysisPaths` 应为 `[]`，即使模型错填一条也不显示
  （`kind !== "answer"` 已过滤）

---

## 2026-04-17 09:27 +0800 — Plan C Step 4：SKILL.md 文档同步

**目的**：把 analysisPaths 这个新字段的行为规则写进 canonical SKILL.md，
避免下次迭代 prompt / skill 时遗忘它。三层一致性闭合。

**改动**：

两份 SKILL.md 各追加新节「## 十、分析路径标签（前端折叠展示）」：

1. `tmp/materialist-dialectics/SKILL.md`（git 追踪）
2. `~/付费专栏/知识库/skills/materialist-dialectics/SKILL.md`（Obsidian canonical）

新节内容：
- 简短定位："这不是贴标签装门面。是让用户看到你真做了什么。"
- 六个合法取值表（与 prompt.js 完全对齐）
- 填写规则 5 条（1–4 条、必须真用到、轻中重档位、按支撑强度排序、
  reject/follow_up 填空数组）
- 四类典型组合（非强制，仅参考）
- 反形式主义提醒

**顺手修掉的旧分歧**：

发现两份 SKILL.md 在本次改动前就有一条小分歧——
canonical 末尾有「如果第五条答案是"更像表演"，就重写」
这句补丁，tracked 副本里没有。趁这次同步顺手把 tracked 也补上，
两份现在 `diff` 返回 IDENTICAL。

**三层一致性现状**：
- SKILL.md（两份）：✅ 一致
- prompt.js（Vercel）：✅ 已部署（需要手动 promote 到 production）
- index.js（Worker）：✅ 已部署（只做代理，无业务逻辑差异）
- 前端 dialectics.js + dialectics.css：✅ 已上线

---

## Plan C 整体小结

✅ **已完成**
- Step 1：后端 schema 加 analysisPaths 字段
- Step 2：prompt 指示模型回填
- Step 3：前端折叠 UI + 导出附带
- Step 4：SKILL.md 文档同步

🎯 **KPI 观察**
待用户自然流量跑一段时间后，观察「本次用到的分析路径」折叠块的展开率：
- < 5% → 用户以工具心态为主，不升级教学模式
- > 15% → 有学习型用户群，考虑把教学模式作为 C 的升级版

初步验证（截图测试）：
- 折叠块正确挂在 disclaimer 之后、消息最末
- 模型填的 3 条路径（具体问题具体分析 / 矛盾分析 / 实践检验）
  都能在正文里找到对应动作，不是贴标签
- 通俗化文案可读

📝 **遗留**
1. disclaimer 字段偶有过度（execution 类不该挂"仅供参考"尾巴），
   属边缘瑕疵，未来可在 prompt 再收紧一次
2. Vercel 生产部署需手动 promote（或把 master 设为生产分支）——
   部署配置问题，下次部署前先根治

---

## 2026-04-17 10:xx +0800 — UI 重设计：Analytical Archive

**目的**：重新设计 dialectics 页面视觉界面，保留所有 JS 功能钩子不变。

**设计方向：「Analytical Archive — 分析档案室」**

血红色代替赭石色，方角几何代替圆角，深化排版层次，增加点阵纹理背景。
目标感：更硬朗、更权威、更具编辑感。

**探索阶段读取的文件**：
- `layouts/_default/dialectics.html`、`assets/css/dialectics.css`、`content/dialectics.md`
- `themes/typo/assets/css/vars.css` — 字体变量（`--font-mono: Monaspace`，`--font-body: Literata`）
- `assets/css/colors/linen.css` — 当前颜色主题（`colorPalette = "linen"`）
- `themes/typo/assets/css/main.css` — CSS 变量映射

**改动**：`assets/css/dialectics.css`（完全重写，799 行 → 新版）

| 设计元素 | 旧设计 | 新设计 |
|---|---|---|
| Accent 颜色 | 暖赭石 `#a14f31` | 血红 `#be2c2c / #e84848` |
| 圆角半径 | `--d-radius-lg: 22px` | `--d-radius-lg: 6px` |
| 标题字号 | `clamp(2.1rem, … 3rem)` | `clamp(2.4rem, … 3.8rem)` |
| 标题装饰 | 无 | 全宽 3px 红色底边线（`::after`） |
| 状态徽章 | 胶囊形 pill | 方形矩形 `border-radius: 2px` |
| Info 面板 | 全四周圆角边框 | 左侧 3px 红色边框，右侧轻圆角 |
| Workspace 卡片 | 顶部细发线边框 | 顶部 3px 红色粗边框 |
| 用户消息 | 圆角聊天气泡 | 硬矩形，切右下角 `2px/2px/0/2px` |
| 回答消息左边框 | 2px | 3px（更粗更醒目） |
| 追问消息 | 全圆角卡片 | 左侧 3px 红色边框，右侧轻圆角 |
| 建议 chips | 圆角卡片 + 悬停上移 | 左边框 list item 风格，悬停填色 |
| 按钮形状 | pill 形 | 方形矩形 `border-radius: 2px` |
| 按钮字体 | 继承 body font | `var(--font-mono)` |
| Intro 背景 | 纯色 | 22px 点阵纹理（`radial-gradient`） |
| 头像样式 | 圆形，无滤镜 | 圆形 + `grayscale(0.35) contrast(1.08)` + 红色 outline |
| 标签页激活态 | 红色底边动画 | 背景填色 + 红色文字 |
| 保护栏列表图标 | `—` 连字符 | `×` 叉号 |
| Scrollbar | 圆角 | 直角 |
| 动画时长 | `0.28s` | `0.24s`（更快更干脆） |

**验证**：
- Hugo 开发服务器（端口 1314）截图确认全部视觉改动正常渲染
- 无控制台错误

**约束保留**：
- 所有 `data-dialectics-*` 钩子完整保留
- 状态类名：`is-user`、`is-system`、`is-archived`、`is-answer`、`is-follow_up`、`is-reject`、`is-online`、`is-offline`、`is-error`、`is-active`
- HTML 结构未改动（仅 CSS 变更）

**Git**：commit `d1c0855`，已推送 origin/master。
